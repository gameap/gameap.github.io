---
title: Справочник config.env
description: "Все переменные окружения GameAP 4 по разделам: база данных, TLS и ACME, аутентификация, кэш, файлы, gRPC, плагины — с типами и значениями по умолчанию."
layout: default
lang: ru
category: Администрирование
order: 334
---

Панель настраивается переменными окружения. Значения читаются из файла `config.env`:

* Linux — `/etc/gameap/config.env`
* Windows — `C:\gameap\web\config.env`

Формат файла — по одной паре `ИМЯ=значение` на строку, без кавычек и без `export`. Строки,
начинающиеся с `#`, игнорируются. После изменения файла панель нужно перезапустить:

```bash
gameapctl panel restart
```

Переменные, заданные в окружении процесса, имеют приоритет над файлом.

Обязательных переменных всего две: `DATABASE_URL` и `AUTH_SECRET`. Без них панель не запустится.
Все остальные имеют значения по умолчанию, пригодные для типовой установки на одном сервере.

Обозначения в таблицах: *длительность* — число с суффиксом `ms`, `s`, `m` или `h` (`100ms`, `30s`,
`5m`, `12h`); *список* — значения через запятую; *размер* — число с двоичным суффиксом `K`, `M`,
`G`, `T`, `P` (`8M`, `100G`), голое число трактуется как байты. Написания `KB` и `KiB` тоже
принимаются; каждый суффикс — степень 1024, дробные значения вида `1.5G` допустимы.

## HTTP

| Переменная             | Тип    | По умолчанию | Назначение                                                                       |
|------------------------|--------|--------------|-----------------------------------------------------------------------------------|
| `HTTP_HOST`            | строка | `0.0.0.0`    | Имя хоста панели. Используется для CORS и как имя в самоподписанных сертификатах  |
| `HTTP_BIND_IP`         | строка | `""`         | IP-адрес, на котором слушать. Пусто — все интерфейсы                              |
| `HTTP_PORT`            | число  | `8025`       | Порт веб-интерфейса и API                                                         |
| `HTTPS_PORT`           | число  | `443`        | Порт HTTPS. Слушается, только если настроен сертификат                            |
| `HTTP_ALLOWED_ORIGINS` | список | `""`         | Источники, которым разрешены запросы из браузера. Пусто — один, выведенный из `HTTP_HOST` |

`HTTP_ALLOWED_ORIGINS` задаётся полными источниками со схемой: `https://panel.example.com`.
Подстановка `*` не поддерживается.

## База данных

| Переменная                 | Тип       | По умолчанию | Назначение                                                  |
|----------------------------|-----------|--------------|--------------------------------------------------------------|
| `DATABASE_DRIVER`          | строка    | `mysql`      | `mysql`, `postgres`, `sqlite`, `inmemory`                    |
| `DATABASE_URL`             | строка    | —            | **Обязательна.** Строка подключения                          |
| `DATABASE_CONNECT_TIMEOUT` | длительн. | `30s`        | Сколько повторять первое подключение к базе при старте       |

Названия драйвера PostgreSQL взаимозаменяемы: `postgres`, `postgresql`, `pgsql`, `pg`, `pgx`.

В течение `DATABASE_CONNECT_TIMEOUT` панель повторяет попытки подключения с нарастающей паузой,
если база пока недоступна — перезапускается при обновлении или стартует позже панели, — вместо
того чтобы сразу завершиться с ошибкой.

Форматы строки подключения:

```text
# PostgreSQL
DATABASE_DRIVER=postgres
DATABASE_URL=postgres://gameap:пароль@localhost:5432/gameap?sslmode=disable

# MySQL / MariaDB
DATABASE_DRIVER=mysql
DATABASE_URL=gameap:пароль@tcp(localhost:3306)/gameap?parseTime=true

# SQLite
DATABASE_DRIVER=sqlite
DATABASE_URL=file:/var/lib/gameap/db.sqlite?_busy_timeout=5000&_journal_mode=WAL&cache=shared
```

Драйвер `inmemory` хранит данные только в оперативной памяти и предназначен для тестов: при
перезапуске всё теряется.

## TLS и сертификаты

| Переменная         | Тип     | По умолчанию | Назначение                                             |
|--------------------|---------|--------------|---------------------------------------------------------|
| `TLS_CERT_FILE`    | строка  | `""`         | Путь к файлу сертификата                                |
| `TLS_KEY_FILE`     | строка  | `""`         | Путь к файлу приватного ключа                           |
| `TLS_CERT`         | строка  | `""`         | Сертификат прямо в переменной, PEM или base64           |
| `TLS_KEY`          | строка  | `""`         | Приватный ключ прямо в переменной, PEM или base64       |
| `TLS_FORCE_HTTPS`  | логич.  | `false`      | Перенаправлять HTTP на HTTPS                            |

HTTPS включается, если задан хотя бы один источник сертификата: файлы, значения в переменных или
ACME. Если не задан ни один, панель работает только по HTTP.

При `TLS_FORCE_HTTPS=true` все запросы по HTTP получают перенаправление `301`, кроме
`/.well-known/acme-challenge/` — иначе выпуск сертификата Let's Encrypt был бы невозможен.

### ACME (Let's Encrypt)

| Переменная                    | Тип        | По умолчанию | Назначение                                                        |
|-------------------------------|------------|--------------|--------------------------------------------------------------------|
| `ACME_ENABLED`                | логич.     | `false`      | Автоматический выпуск сертификата                                  |
| `ACME_EMAIL`                  | строка     | `""`         | Адрес для уведомлений. **Обязателен** при включённом ACME           |
| `ACME_DOMAINS`                | список     | `""`         | Домены сертификата. **Обязательны** при включённом ACME             |
| `ACME_CHALLENGE_TYPE`         | строка     | `http-01`    | `http-01` или `dns-01`                                             |
| `ACME_DNS_PROVIDER`           | строка     | `""`         | Провайдер DNS для `dns-01`, например `cloudflare`                  |
| `ACME_DIRECTORY_URL`          | строка     | боевой ACME  | Адрес каталога ACME. Для тестов укажите staging Let's Encrypt       |
| `ACME_RENEWAL_THRESHOLD`      | длительн.  | `720h`       | За сколько до истечения обновлять. По умолчанию 30 суток            |
| `ACME_RENEWAL_CHECK_INTERVAL` | длительн.  | `12h`        | Как часто проверять срок                                            |
| `ACME_PROPAGATION_TIMEOUT`    | длительн.  | `180s`       | Сколько ждать распространения DNS-записи при `dns-01`               |
| `ACME_STORAGE_PATH`           | строка     | `acme`       | Каталог хранения сертификатов и ключа учётной записи ACME           |

ACME включается только при одновременно заданных `ACME_ENABLED=true`, `ACME_EMAIL` и `ACME_DOMAINS`.
Если чего-то не хватает, панель молча продолжит работу без ACME.

По умолчанию используется боевой каталог Let's Encrypt с жёсткими ограничениями на число попыток.
При отладке настройки указывайте staging: `ACME_DIRECTORY_URL=https://acme-staging-v02.api.letsencrypt.org/directory`.

## Аутентификация

| Переменная                      | Тип       | По умолчанию | Назначение                                                               |
|---------------------------------|-----------|--------------|---------------------------------------------------------------------------|
| `AUTH_SECRET`                   | строка    | —            | **Обязательна.** Ключ подписи токенов, ровно 32 случайных байта            |
| `ENCRYPTION_KEY`                | строка    | `""`         | Ключ шифрования секретов в базе, рекомендуется 32 случайных байта         |
| `AUTH_SERVICE`                  | строка    | `paseto`     | Формат токенов: `paseto` или `jwt`. Иное значение — панель не запустится   |
| `AUTH_BCRYPT_COST`              | число     | `13`         | Стоимость bcrypt, от 10 до 14                                             |
| `AUTH_ALLOW_WEAK_PASSWORDS`     | логич.    | `false`      | Отключает проверку по списку скомпрометированных паролей                  |
| `AUTH_REQUIRE_MFA_FOR_ADMINS`   | логич.    | `true`       | Требовать 2FA от администраторов                                          |
| `AUTH_MFA_HARD_FAIL_DAYS`       | число     | `30`         | Дней до блокировки. `0` — только напоминание                              |
| `AUTH_MFA_ENROLLMENT_TOKEN_TTL` | длительн. | `15m`        | Срок ограниченной сессии, выдаваемой после дедлайна                       |
| `AUTH_SHORT_LIVED_TOKEN_TTL`    | длительн. | `10s`        | Срок одноразовых токенов `glst_`. Фактически ограничен 10 секундами        |
| `AUTH_SSO_TICKET_TTL`           | длительн. | `60s`        | Срок одноразового SSO-билета для входа. Ограничен 120 секундами            |

Подробности — на странице [Безопасность](/ru/security.html).

SSO-билет выпускается для другого пользователя, и внешняя система — например, биллинг — обменивает
его на авторизованную сессию. Обработчик ограничивает срок 120 секундами; нулевое или отрицательное
значение заменяется на 60 секунд. При нескольких экземплярах панели билет выпускается на одном
экземпляре, а погашается на другом, поэтому нужен общий `CACHE_DRIVER`: `redis`, `mysql` или
`postgres`, но не `memory`.

> `AUTH_SECRET` приводится к 32 байтам молча: короткое значение дополняется, длинное обрезается.
> Задавайте ровно 32 символа, например `openssl rand -base64 24`.

### Первый администратор

Эти три переменные читаются только при первоначальном заполнении пустой базы:

| Переменная       | Назначение                                                                     |
|------------------|---------------------------------------------------------------------------------|
| `ADMIN_LOGIN`    | Логин первого администратора                                                    |
| `ADMIN_EMAIL`    | Адрес электронной почты                                                          |
| `ADMIN_PASSWORD` | Пароль. Если не задан, генерируется случайный и выводится в журнал при первом запуске |

> Пароль из `ADMIN_PASSWORD` **не проходит** проверку политики паролей: ни по длине, ни по списку
> скомпрометированных. Задавайте его осознанно.

## Права доступа и кэш

| Переменная                  | Тип       | По умолчанию     | Назначение                                    |
|-----------------------------|-----------|------------------|------------------------------------------------|
| `RBAC_CACHE_TTL`            | длительн. | `30s`            | Время жизни кэша проверок прав                 |
| `CACHE_DRIVER`              | строка    | `memory`         | `memory`, `redis`, `postgres`, `mysql`. См. ниже |
| `CACHE_REDIS_ADDR`          | строка    | `localhost:6379` | Адрес Redis                                    |
| `CACHE_REDIS_PASSWORD`      | строка    | `""`             | Пароль Redis                                   |
| `CACHE_REDIS_DB`            | число     | `0`              | Номер базы Redis                               |
| `CACHE_TTL_RBAC`            | длительн. | `24h`            | Время жизни кэша прав                          |
| `CACHE_TTL_GAMES`           | длительн. | `48h`            | Время жизни кэша игр                           |
| `CACHE_TTL_NODES`           | длительн. | `24h`            | Время жизни кэша выделенных серверов           |
| `CACHE_TTL_USERS`           | длительн. | `6h`             | Время жизни кэша пользователей                 |
| `CACHE_TTL_PERSONAL_TOKENS` | длительн. | `24h`            | Время жизни кэша персональных токенов          |
| `CACHE_TTL_SERVER_SETTINGS` | длительн. | `12h`            | Время жизни кэша настроек серверов             |

`CACHE_DRIVER` принимает `memory` (синоним `inmemory`), `redis`, `mysql` (синоним `database`) и
`postgres` (синонимы `postgresql`, `pgsql`, `pg`). Неизвестное значение приводит к аварийному
завершению при старте.

Кэш хранит не только справочные данные: в нём живут ключ установки демона, список отозванных токенов
и счётчики попыток входа. При `CACHE_DRIVER=memory` всё это теряется при перезапуске и не разделяется
между несколькими экземплярами панели. Для установки из нескольких экземпляров используйте `redis`.

## Файлы

| Переменная                  | Тип    | По умолчанию | Назначение                                                     |
|-----------------------------|--------|--------------|-----------------------------------------------------------------|
| `FILES_DRIVER`              | строка | `local`      | `local` или `s3`                                                |
| `FILES_LOCAL_BASE_PATH`     | строка | `""`         | Базовый каталог для драйвера `local`                            |
| `FILES_S3_ENDPOINT`         | строка | `""`         | Адрес S3-совместимого хранилища                                 |
| `FILES_S3_USE_SSL`          | логич. | `true`       | Обращаться к хранилищу по HTTPS                                 |
| `FILES_S3_ACCESS_KEY_ID`    | строка | `""`         | Идентификатор ключа доступа                                     |
| `FILES_S3_SECRET_ACCESS_KEY`| строка | `""`         | Секретный ключ доступа                                          |
| `FILES_S3_BUCKET`           | строка | `""`         | Имя бакета                                                      |

### Загрузка файлов

| Переменная                      | Тип       | По умолчанию | Назначение                                                    |
|---------------------------------|-----------|--------------|----------------------------------------------------------------|
| `FILES_UPLOAD_CHUNK_SIZE`       | размер    | `8M`         | Размер части при загрузке по частям                            |
| `FILES_UPLOAD_MAX_CHUNKS`       | число     | `100000`     | Предельное число частей на один файл                           |
| `FILES_UPLOAD_SESSION_TTL`      | длительн. | `24h`        | Сколько живёт незавершённая загрузка                           |
| `FILES_UPLOAD_DISPATCH_TIMEOUT` | длительн. | `2m`         | Таймаут передачи собранного файла демону                       |
| `FILES_UPLOAD_JANITOR_INTERVAL` | длительн. | `12h`        | Как часто убирать просроченные загрузки                        |
| `FILES_UPLOAD_ALLOWED_MIMES`    | список    | `""`         | Дополняет список разрешённых типов, не заменяет его            |
| `FILES_UPLOAD_ALLOW_ARCHIVES`   | логич.    | `false`      | Разрешить архивы: zip, tar, gzip, bzip2, 7z, xz                |
| `FILES_UPLOAD_ALLOW_BINARY`     | логич.    | `false`      | Разрешить произвольные двоичные файлы                          |

Предельный размер файла — произведение `FILES_UPLOAD_CHUNK_SIZE` на `FILES_UPLOAD_MAX_CHUNKS`.
При значениях по умолчанию это около 780 ГБ. Отдельно действует ограничение в 100 МБ на обычную
загрузку одним запросом, оно не настраивается.

### Архивы

| Переменная                            | Тип    | По умолчанию | Назначение                                                                                   |
|---------------------------------------|--------|--------------|-----------------------------------------------------------------------------------------------|
| `FILES_ARCHIVE_MAX_BYTES`             | размер | `100G`       | Предельный размер данных одной операции с архивом в несжатом виде                             |
| `FILES_ARCHIVE_MAX_FILES`             | число  | `500000`     | Предельное число записей в одной операции с архивом                                           |
| `FILES_ARCHIVE_CONCURRENT_PER_SERVER` | число  | `2`          | Одновременных ZIP-скачиваний каталога на один игровой сервер. При превышении — HTTP `429`     |

`FILES_ARCHIVE_MAX_BYTES` и `FILES_ARCHIVE_MAX_FILES` ограничивают потоковое скачивание каталога в
виде ZIP, а начиная с 4.5.0 — ещё и создание и распаковку архивов, которые
[файловый менеджер](/ru/gameap_configure/file_manager.html) выполняет на выделенном сервере; при
распаковке ограничение размера защищает от «архивных бомб». Значение `0` снимает ограничение при
потоковом скачивании, а для операции на выделенном сервере оставляет предел на усмотрение демона,
который применяет собственные значения: 10 ГиБ и 100 000 записей.
`FILES_ARCHIVE_CONCURRENT_PER_SERVER` действует только на скачивание и не ограничивает создание и
распаковку на выделенном сервере.

## gRPC

| Переменная                    | Тип    | По умолчанию | Назначение                                            |
|-------------------------------|--------|--------------|--------------------------------------------------------|
| `GRPC_PORT`                   | число  | `31718`      | Порт gRPC-сервера для демонов                          |
| `GRPC_TLS_ENABLED`            | логич. | `true`       | Шифрование соединения с демонами                       |
| `GRPC_REQUIRE_MTLS`           | логич. | `false`      | Требовать от демона клиентский сертификат              |
| `GRPC_EXTERNAL_HOST`          | строка | `""`         | Адрес панели, сообщаемый демону                        |
| `GRPC_EXTERNAL_PORT`          | число  | `0`          | Порт, сообщаемый демону. `0` — берётся `GRPC_PORT`     |
| `GRPC_MAX_RECV_MSG_SIZE`      | число  | `10485760`   | Предельный размер входящего сообщения, байт            |
| `GRPC_MAX_SEND_MSG_SIZE`      | число  | `10485760`   | Предельный размер исходящего сообщения, байт           |
| `GRPC_MAX_CONCURRENT_STREAMS` | число  | `100`        | Одновременных потоков на соединение                    |
| `GRPC_ENABLE_REFLECTION`      | логич. | `false`      | Отражение схемы для отладочных утилит                  |
| `DAEMON_SETUP_KEY`            | строка | `""`         | Постоянный ключ установки демона вместо временного     |

Подробности — на странице [GRPC API](/ru/daemon/grpc.html).

> `DAEMON_SETUP_KEY` задаёт ключ, который не истекает. Это удобно для автоматического развёртывания,
> но такой ключ позволяет зарегистрировать в панели новый выделенный сервер кому угодно, кто его
> узнает. Для обычной установки не задавайте эту переменную — панель выпустит временный ключ на час.

## Безопасность

Полное описание — на странице [Безопасность](/ru/security.html).

| Переменная                          | Тип    | По умолчанию                      | Назначение                                    |
|-------------------------------------|--------|-----------------------------------|------------------------------------------------|
| `SECURITY_HEADERS_ENABLED`          | логич. | `true`                            | Общий выключатель заголовков безопасности      |
| `SECURITY_CONTENT_TYPE_OPTIONS`     | логич. | `true`                            | `X-Content-Type-Options: nosniff`              |
| `SECURITY_FRAME_OPTIONS`            | строка | `SAMEORIGIN`                      | `X-Frame-Options`                              |
| `SECURITY_REFERRER_POLICY`          | строка | `strict-origin-when-cross-origin` | `Referrer-Policy`                              |
| `SECURITY_HSTS_ENABLED`             | логич. | `true`                            | HSTS. Отдаётся только по HTTPS                 |
| `SECURITY_HSTS_MAX_AGE`             | число  | `31536000`                        | Срок HSTS в секундах                           |
| `SECURITY_HSTS_INCLUDE_SUBDOMAINS`  | логич. | `false`                           | Распространить HSTS на поддомены               |
| `SECURITY_HSTS_PRELOAD`             | логич. | `false`                           | Добавить `preload`                             |
| `SECURITY_CSP_ENABLED`              | логич. | `true`                            | Content Security Policy                        |
| `SECURITY_CSP_REPORT_ONLY`          | логич. | `false`                           | Только отчёты, без блокировки                  |
| `SECURITY_CSP_POLICY`               | строка | `""`                              | Полностью заменяет генерируемую политику       |
| `SECURITY_CSP_REPORT_URI`           | строка | `""`                              | Адрес для отчётов CSP                          |
| `SECURITY_CSP_EXTRA_SCRIPT_SRC`     | список | `""`                              | Дополняет `script-src`                         |
| `SECURITY_CSP_EXTRA_STYLE_SRC`      | список | `""`                              | Дополняет `style-src`                          |
| `SECURITY_CSP_EXTRA_CONNECT_SRC`    | список | `""`                              | Дополняет `connect-src`                        |
| `SECURITY_CSP_EXTRA_IMG_SRC`        | список | `""`                              | Дополняет `img-src`                            |
| `SECURITY_CSP_EXTRA_FRAME_SRC`      | список | `""`                              | Дополняет `frame-src`                          |
| `SECURITY_CSP_EXTRA_FONT_SRC`       | список | `""`                              | Дополняет `font-src`                           |
| `SECURITY_SENSITIVE_PATH_PREFIXES`  | список | см. ниже                          | Пути, ответы по которым запрещено кэшировать   |

Значение `SECURITY_SENSITIVE_PATH_PREFIXES` по умолчанию:
`/api/auth/,/api/profile/,/api/users/,/api/tokens/`.

### CAPTCHA

| Переменная           | Тип     | По умолчанию | Назначение                                                      |
|----------------------|---------|--------------|------------------------------------------------------------------|
| `CAPTCHA_PROVIDER`   | строка  | `""`         | `recaptcha_v2`, `recaptcha_v3` или `turnstile`. Пусто — выключена |
| `CAPTCHA_SITE_KEY`   | строка  | `""`         | Публичный ключ                                                   |
| `CAPTCHA_SECRET_KEY` | строка  | `""`         | Секретный ключ                                                   |
| `CAPTCHA_MIN_SCORE`  | дробное | `0.5`        | Порог только для reCAPTCHA v3                                    |
| `CAPTCHA_FAIL_OPEN`  | логич.  | `false`      | Пускать вход, если сервис проверки недоступен                    |
| `CAPTCHA_VERIFY_URL` | строка  | `""`         | Свой адрес проверки                                              |

### Журнал аудита

| Переменная               | Тип    | По умолчанию | Назначение                                |
|--------------------------|--------|--------------|--------------------------------------------|
| `AUDIT_ENABLED`          | логич. | `true`       | Запись событий безопасности                |
| `AUDIT_CLIENT_IP_HEADER` | строка | `""`         | Заголовок с реальным IP клиента            |

## Плагины

> Начиная с 4.5.0 все настройки плагинов пишутся как `PLUGINS_*`. Прежние имена `PLUGIN_*`, а также
> `PLUGINS_CACHE_ENABLED` и `PLUGINS_CACHE_DIR` продолжают работать один релиз: панель применяет
> значение и пишет в журнал `environment variable is deprecated and will be removed in a future release`
> с именем замены. Если заданы и старое, и новое имя, старое игнорируется. Исключение — три имени,
> содержавшие единицу измерения: `PLUGIN_HTTP_MAX_TIMEOUT_SECONDS`, `PLUGIN_NET_MAX_TIMEOUT_SECONDS` и
> `PLUGIN_NET_READ_BUFFER_BYTES` больше не распознаются и молча игнорируются — замените их на
> `PLUGINS_HTTP_MAX_TIMEOUT=30s`, `PLUGINS_NET_MAX_TIMEOUT=10s` и `PLUGINS_NET_READ_BUFFER=64K`.
> `gameapctl panel upgrade` переименовывает все эти переменные в `config.env` автоматически.

| Переменная                  | Тип    | По умолчанию                      | Назначение                                                                                                    |
|-----------------------------|--------|-----------------------------------|----------------------------------------------------------------------------------------------------------------|
| `PLUGINS_DISABLED`          | логич. | `false`                           | Полностью отключить плагины                                                                                    |
| `PLUGINS_AUTOLOAD`          | список | `""`                              | Плагины, загружаемые при старте                                                                                |
| `PLUGINS_STRICT_LOAD`       | логич. | `false`                           | Не запускаться, если хотя бы один плагин не загрузился. Выключено: плагин получает статус `error` и пропускается |
| `PLUGINS_STORE_URL`         | строка | `https://plugins.gameap.dev/api`  | Адрес каталога плагинов                                                                                        |
| `PLUGINS_STORE_LICENSE_KEY` | строка | `""`                              | Лицензионный ключ для платных плагинов                                                                         |

### Среда выполнения плагинов

| Переменная                        | Тип    | По умолчанию | Назначение                                                                          |
|-----------------------------------|--------|--------------|--------------------------------------------------------------------------------------|
| `PLUGINS_RUNTIME_MAX_MEMORY`      | размер | `256M`       | Предел памяти на модуль плагина. `0` — значение wazero по умолчанию, 4 ГиБ           |
| `PLUGINS_RUNTIME_MAX_MODULE_SIZE` | размер | `128M`       | Наибольший файл `.wasm`, принимаемый при установке и загрузке. `0` — без предела     |
| `PLUGINS_RUNTIME_CACHE_ENABLED`   | логич. | `true`       | Кэш скомпилированных модулей WebAssembly                                             |
| `PLUGINS_RUNTIME_CACHE_DIR`       | строка | `""`         | Каталог этого кэша. Если задан, скомпилированный код переживает перезапуск панели    |

Модуль, объявляющий больший максимум памяти, ограничивается значением `PLUGINS_RUNTIME_MAX_MEMORY`;
не загрузится только модуль, чья начальная память уже превышает предел. `PLUGINS_RUNTIME_MAX_MODULE_SIZE`
действует одинаково для загрузки через интерфейс, установки из каталога и автозагрузки; плагин,
загружаемый через веб-интерфейс, дополнительно ограничен 100 МБ независимо от этого значения.

### Сетевые запросы плагинов

| Переменная                               | Тип       | По умолчанию | Назначение                                            |
|------------------------------------------|-----------|--------------|--------------------------------------------------------|
| `PLUGINS_HTTP_BLOCK_PRIVATE_IPS`         | логич.    | `true`       | Запрет обращений к внутренним адресам сети             |
| `PLUGINS_HTTP_ALLOWED_SCHEMES`           | список    | `https`      | Разрешённые схемы                                      |
| `PLUGINS_HTTP_ALLOWED_HOSTS`             | список    | `""`         | Хосты-исключения из запрета внутренних адресов         |
| `PLUGINS_HTTP_MAX_TIMEOUT`               | длительн. | `30s`        | Предельное время запроса                               |
| `PLUGINS_HTTP_MAX_REDIRECTS`             | число     | `5`          | Предельное число перенаправлений                       |
| `PLUGINS_HTTP_RESPONSE_HEADER_ALLOWLIST` | список    | `""`         | Дополняет список заголовков, передаваемых плагину      |

Адреса служб метаданных облачных провайдеров заблокированы всегда, `PLUGINS_HTTP_ALLOWED_HOSTS`
на них не действует.

Отдельная группа настроек управляет собственными протоколами RCON и Query, реализуемыми плагинами:

| Переменная                      | Тип       | По умолчанию | Назначение                                              |
|---------------------------------|-----------|--------------|----------------------------------------------------------|
| `PLUGINS_NET_ENABLED`           | логич.    | `true`       | Разрешить плагинам сетевой обмен с игровыми серверами    |
| `PLUGINS_NET_BLOCK_PRIVATE_IPS` | логич.    | `false`      | Запрет внутренних адресов. Выключен: игровые серверы часто во внутренней сети |
| `PLUGINS_NET_ALLOWED_HOSTS`     | список    | `""`         | Хосты-исключения                                         |
| `PLUGINS_NET_MAX_TIMEOUT`       | длительн. | `10s`        | Предельное время одной операции                          |
| `PLUGINS_NET_READ_BUFFER`       | размер    | `64K`        | Предельный размер одного чтения                          |
| `PLUGINS_NET_MAX_CONNECTIONS`   | число     | `8`          | Одновременных соединений на плагин                       |

### Планировщик плагинов

| Переменная                               | Тип       | По умолчанию | Назначение                                        |
|------------------------------------------|-----------|--------------|----------------------------------------------------|
| `PLUGINS_SCHEDULER_MIN_INTERVAL`         | длительн. | `1s`         | Наименьший допустимый интервал задачи              |
| `PLUGINS_SCHEDULER_MAX_TASKS_PER_PLUGIN` | число     | `32`         | Предельное число задач на плагин                   |
| `PLUGINS_SCHEDULER_CALL_TIMEOUT`         | длительн. | `60s`        | Таймаут вызова обработчика по умолчанию            |
| `PLUGINS_SCHEDULER_MAX_CALL_TIMEOUT`     | длительн. | `5m`         | Предельный таймаут, который может запросить плагин |
| `PLUGINS_SCHEDULER_MAX_RETRIES`          | число     | `10`         | Предельное число повторов                          |
| `PLUGINS_SCHEDULER_MAX_RETRY_DELAY`      | длительн. | `10m`        | Предельная пауза между повторами                   |
| `PLUGINS_SCHEDULER_MAX_JITTER`           | длительн. | `30s`        | Предельный случайный разброс запуска               |
| `PLUGINS_SCHEDULER_REFRESH_INTERVAL`     | длительн. | `30s`        | Как часто перечитывать задачи из базы              |

### Права плагинов

| Переменная                      | Тип       | По умолчанию | Назначение                                                                                    |
|---------------------------------|-----------|--------------|------------------------------------------------------------------------------------------------|
| `PLUGINS_PERMISSIONS_ENFORCE`   | логич.    | `false`      | Применять записанные разрешения плагинов                                                       |
| `PLUGINS_PERMISSIONS_CACHE_TTL` | длительн. | `30s`        | Сколько разрешения плагина хранятся в памяти. `0` — читать запись плагина при каждой проверке  |

В этом выпуске проверка выключена, чтобы плагины, написанные до появления разрешений, продолжали
работать. При `false` ни одна проверка разрешений ничего не блокирует; сами разрешения по-прежнему
записываются, отображаются и редактируются в панели. Изменения разрешений рассылаются всем
экземплярам через pub/sub, поэтому время жизни кэша ограничивает расхождение только на время
недоступности брокера.

### Восстановление плагинов

| Переменная                       | Тип       | По умолчанию | Назначение                                                          |
|----------------------------------|-----------|--------------|----------------------------------------------------------------------|
| `PLUGINS_RECOVERY_ENABLED`       | логич.    | `true`       | Автоматически перезагружать плагины, отключённые средой выполнения   |
| `PLUGINS_RECOVERY_INITIAL_DELAY` | длительн. | `30s`        | Пауза перед первой попыткой                                          |
| `PLUGINS_RECOVERY_MAX_DELAY`     | длительн. | `10m`        | Верхняя граница экспоненциально растущей паузы                       |
| `PLUGINS_RECOVERY_MAX_ATTEMPTS`  | число     | `5`          | Число попыток                                                        |

Плагин, отключённый средой выполнения — вызов превысил свой срок или модуль сам завершил работу, —
перезагружается с экспоненциально растущей паузой. После последней попытки он остаётся в статусе
`error`, пока оператор не перезагрузит его или панель не перезапустится. При
`PLUGINS_RECOVERY_ENABLED=false` отключение по-прежнему фиксируется (статус, причина, журнал аудита),
но автоматической перезагрузки нет.

### Синхронизация плагинов

| Переменная                      | Тип       | По умолчанию | Назначение                                                                                    |
|---------------------------------|-----------|--------------|------------------------------------------------------------------------------------------------|
| `PLUGINS_SYNC_DISABLED`         | логич.    | `false`      | Отключить синхронизацию изменений плагинов между экземплярами                                  |
| `PLUGINS_SYNC_REFRESH_INTERVAL` | длительн. | `60s`        | Период сверки; ограничивает, как долго потерянное уведомление оставляет экземпляр устаревшим    |
| `PLUGINS_SYNC_MIN_BACKOFF`      | длительн. | `15s`        | Наименьшая пауза перед повтором, если плагин не загрузился на этом экземпляре                   |
| `PLUGINS_SYNC_MAX_BACKOFF`      | длительн. | `15m`        | Наибольшая пауза перед повтором                                                                |

Актуально для установки из [нескольких экземпляров](/ru/multi_instance.html): установка,
обновление, удаление, перезагрузка и изменение разрешений, выполненные на одном экземпляре, доходят
до остальных через уведомление pub/sub и периодическую сверку. При выключенной синхронизации
изменения попадают на экземпляр только после его перезапуска.

### Доступ плагинов к файлам узлов

| Переменная                     | Тип    | По умолчанию   | Назначение                                                                                                |
|--------------------------------|--------|----------------|------------------------------------------------------------------------------------------------------------|
| `PLUGINS_NODEFS_MAX_INLINE`    | размер | `32M`          | Наибольший файл, который плагин может скачать или загрузить одним вызовом. `0` — без предела              |
| `PLUGINS_NODEFS_PATH_POLICY`   | строка | `unrestricted` | Куда плагины могут направлять файловые операции на узлах: `unrestricted`, `node_workpath`, `server_dirs`  |
| `PLUGINS_NODEFS_ALLOWED_PATHS` | список | `""`           | Дополнительные абсолютные корни, разрешённые на каждом узле в ограниченных режимах                        |

`node_workpath` ограничивает пути рабочим каталогом выделенного сервера, `server_dirs` — каталогами
игровых серверов на этом узле. Политика распространяется и на рабочий каталог команд, которые
плагины выполняют на узлах. Сегменты `..` отклоняются в любом режиме.

### Хранилище и кэш плагинов

| Переменная                            | Тип    | По умолчанию | Назначение                                                 |
|---------------------------------------|--------|--------------|-------------------------------------------------------------|
| `PLUGINS_STORAGE_MAX_KEYS_PER_PLUGIN` | число  | `10000`      | Число записей, которые может хранить один плагин            |
| `PLUGINS_STORAGE_MAX_VALUE`           | размер | `1M`         | Наибольшее одно сохраняемое значение                        |
| `PLUGINS_STORAGE_MAX_TOTAL`           | размер | `64M`        | Суммарный размер всех значений одного плагина               |
| `PLUGINS_CACHE_MAX_VALUE`             | размер | `1M`         | Наибольшее значение в кэше плагинов. `0` — без предела      |

Квоты хранилища нельзя отключить: нулевое или отрицательное значение заменяется встроенным
значением по умолчанию. Кэш плагинов живёт в кэше панели (`CACHE_DRIVER`), у каждого плагина своё
пространство ключей, и при удалении плагина он не очищается.

### Секреты плагинов

| Переменная                            | Тип    | По умолчанию | Назначение                                            |
|---------------------------------------|--------|--------------|--------------------------------------------------------|
| `PLUGINS_SECRETS_MAX_KEYS_PER_PLUGIN` | число  | `64`         | Число секретов, которые может хранить один плагин      |
| `PLUGINS_SECRETS_MAX_VALUE`           | размер | `8K`         | Наибольший размер одного секрета в открытом виде       |
| `PLUGINS_SECRETS_REQUIRE_ENCRYPTION`  | логич. | `true`       | Отклонять запись, пока не задан `ENCRYPTION_KEY`       |

Секреты плагинов хранятся зашифрованными ключом `ENCRYPTION_KEY`. При
`PLUGINS_SECRETS_REQUIRE_ENCRYPTION=false` и незаданном ключе секреты записываются в открытом виде —
не безопаснее обычного хранилища плагинов.

### SSH для плагинов

| Переменная                              | Тип       | По умолчанию | Назначение                                                                                        |
|-----------------------------------------|-----------|--------------|----------------------------------------------------------------------------------------------------|
| `PLUGINS_SSH_ENABLED`                   | логич.    | `false`      | Разрешить плагинам открывать SSH-соединения с панели                                               |
| `PLUGINS_SSH_BLOCK_PRIVATE_IPS`         | логич.    | `true`       | Запрет подключений к внутренним адресам сети                                                       |
| `PLUGINS_SSH_ALLOWED_HOSTS`             | список    | `""`         | Хосты-исключения из запрета внутренних адресов                                                     |
| `PLUGINS_SSH_ALLOW_ACCEPT_ANY_HOST_KEY` | логич.    | `true`       | Разрешить политику ключа хоста `accept_any` (доверие при первом подключении)                       |
| `PLUGINS_SSH_MAX_CONNECTIONS`           | число     | `8`          | Одновременных соединений на плагин                                                                 |
| `PLUGINS_SSH_MAX_OPERATIONS`            | число     | `16`         | Одновременно выполняемых команд на плагин                                                          |
| `PLUGINS_SSH_CONNECT_TIMEOUT`           | длительн. | `30s`        | Подключение, рукопожатие и аутентификация вместе; также потолок для собственного значения плагина  |
| `PLUGINS_SSH_MAX_EXEC_TIMEOUT`          | длительн. | `30m`        | Потолок для одной удалённой команды                                                                |
| `PLUGINS_SSH_IDLE_TIMEOUT`              | длительн. | `10m`        | Закрывать соединение, на котором столько времени ничего не выполнялось                             |
| `PLUGINS_SSH_MAX_OUTPUT_BYTES`          | число     | `1048576`    | Сохраняемые stdout и stderr одной команды, байт. Начало сохраняется, остальное обрезается           |
| `PLUGINS_SSH_MAX_STDIN_BYTES`           | число     | `1048576`    | Данные, которые плагин может передать команде на stdin, байт                                       |
| `PLUGINS_SSH_OPERATION_RETENTION`       | длительн. | `10m`        | Сколько завершённая операция с её выводом остаётся доступной для чтения                            |
| `PLUGINS_SSH_MAX_RETAINED_OPERATIONS`   | число     | `64`         | Хранимых завершённых операций на плагин; старейшие вытесняются первыми                             |
| `PLUGINS_SSH_KEEPALIVE_INTERVAL`        | длительн. | `30s`        | Интервал проверок живости открытых соединений                                                      |
| `PLUGINS_SSH_COMPLETION_CALL_TIMEOUT`   | длительн. | `30s`        | Таймаут одного обратного вызова плагина о завершении. Плагин, не ответивший вовремя, отключается   |
| `PLUGINS_SSH_BUSY_RETRY_DELAY`          | длительн. | `2s`         | Пауза между повторами обратного вызова, пока плагин занят другим вызовом                           |
| `PLUGINS_SSH_BUSY_RETRIES`              | число     | `5`          | Число повторов обратного вызова, если плагин занят                                                 |

Хост-библиотека `gameap-ssh` позволяет плагинам подключаться к хостам по SSH и выполнять там
команды — например, чтобы установить GameAP Daemon на новую машину. По умолчанию она выключена, и
`PLUGINS_SSH_ENABLED` — осознанное согласие оператора на исходящие SSH-подключения с панели. Адреса
служб метаданных облачных провайдеров заблокированы даже при `PLUGINS_SSH_BLOCK_PRIVATE_IPS=false`.
Соединения и операции живут в памяти того экземпляра панели, который их открыл.

### Ограничения частоты вызовов плагинов

Ограничения по алгоритму token bucket на плагин и на экземпляр панели для ресурсоёмких
хост-библиотек: установившаяся частота вызовов в секунду (`_RPS`) плюс всплеск (`_BURST`).
Отклонённый вызов получает в ответе ошибку «rate limited»; плагин за это не отключается. `_RPS`,
равный `0`, снимает ограничение для этого класса.

| Переменная                              | Тип     | По умолчанию | Назначение                                                                                            |
|-----------------------------------------|---------|--------------|--------------------------------------------------------------------------------------------------------|
| `PLUGINS_RATELIMIT_NODECMD_RPS`         | дробное | `5`          | Команды на выделенных серверах (`gameap-nodecmd`)                                                      |
| `PLUGINS_RATELIMIT_NODECMD_BURST`       | число   | `20`         | Всплеск для того же класса                                                                             |
| `PLUGINS_RATELIMIT_SERVERCONTROL_RPS`   | дробное | `5`          | Управление игровыми серверами, задачи демона, запись серверов и их настроек (`gameap-servercontrol`)   |
| `PLUGINS_RATELIMIT_SERVERCONTROL_BURST` | число   | `20`         | Всплеск для того же класса                                                                             |
| `PLUGINS_RATELIMIT_NODEFS_RPS`          | дробное | `50`         | Файловые операции на узлах (`gameap-nodefs`)                                                           |
| `PLUGINS_RATELIMIT_NODEFS_BURST`        | число   | `200`        | Всплеск для того же класса                                                                             |
| `PLUGINS_RATELIMIT_HTTP_RPS`            | дробное | `20`         | Исходящие HTTP-запросы (`gameap-http`)                                                                 |
| `PLUGINS_RATELIMIT_HTTP_BURST`          | число   | `50`         | Всплеск для того же класса                                                                             |
| `PLUGINS_RATELIMIT_RBAC_RPS`            | дробное | `10`         | Запись ролей и прав (`gameap-rbac`)                                                                    |
| `PLUGINS_RATELIMIT_RBAC_BURST`          | число   | `50`         | Всплеск для того же класса                                                                             |
| `PLUGINS_RATELIMIT_SSH_RPS`             | дробное | `20`         | Каждый SSH-вызов, включая опрос выполняющейся команды (`gameap-ssh`)                                   |
| `PLUGINS_RATELIMIT_SSH_BURST`           | число   | `60`         | Всплеск для того же класса                                                                             |

## Обмен событиями между экземплярами

Нужен только при работе нескольких экземпляров панели. При одном экземпляре достаточно значений
по умолчанию. Неизвестное значение `PUBSUB_DRIVER` приводит к аварийному завершению при старте.

| Переменная                   | Тип     | По умолчанию | Назначение                                                  |
|------------------------------|---------|--------------|--------------------------------------------------------------|
| `PUBSUB_DRIVER`              | строка  | `memory`     | `memory`, `redis`, `postgres`                                |
| `PUBSUB_INSTANCE_ID`         | строка  | `""`         | Идентификатор экземпляра, должен быть уникальным             |
| `PUBSUB_REDIS_ADDR`          | строка  | `""`         | Адрес Redis                                                  |
| `PUBSUB_REDIS_PASSWORD`      | строка  | `""`         | Пароль Redis                                                 |
| `PUBSUB_REDIS_DB`            | число   | `1`          | Номер базы Redis                                             |
| `PUBSUB_RETRY_ENABLED`       | логич.  | `true`       | Повторять доставку при ошибке                                |
| `PUBSUB_RETRY_MAX_RETRIES`   | число   | `3`          | Предельное число повторов                                    |
| `PUBSUB_RETRY_INITIAL_DELAY` | длительн.| `100ms`     | Начальная пауза перед повтором                               |
| `PUBSUB_RETRY_MAX_DELAY`     | длительн.| `5s`        | Предельная пауза перед повтором                              |
| `PUBSUB_RETRY_MULTIPLIER`    | дробное | `2.0`        | Во сколько раз увеличивать паузу                             |
| `PUBSUB_DLQ_ENABLED`         | логич.  | `false`      | Складывать недоставленные события в отдельную очередь        |
| `PUBSUB_DLQ_DRIVER`          | строка  | `memory`     | Хранилище этой очереди                                       |
| `PUBSUB_DLQ_MAX_SIZE`        | число   | `1000`       | Предельный размер очереди                                    |

## Прочее

| Переменная                    | Тип       | По умолчанию               | Назначение                                               |
|-------------------------------|-----------|----------------------------|-----------------------------------------------------------|
| `LOGGER_LEVEL`                | строка    | `info`                     | `debug`, `info`, `warn`, `error`                          |
| `LOGGER_LOG_DB_QUERIES`       | логич.    | `false`                    | Записывать запросы к базе. Только для отладки             |
| `DEFAULT_LANGUAGE`            | строка    | `""`                       | Язык интерфейса по умолчанию: `en`, `ru`, `es`, `de`. Пусто — язык браузера    |
| `GLOBAL_API_URL`              | строка    | `https://api.gameap.com`   | Адрес Global API — отчёты об ошибках                      |
| `GAMES_CDN_URLS`              | список    | см. ниже                   | Источники каталога игр, перебираются по порядку           |
| `TASK_REAPER_INTERVAL`        | длительн. | `1m`                       | Как часто искать зависшие задачи                          |
| `TASK_REAPER_STALE_THRESHOLD` | длительн. | `10m`                      | После какого простоя задача считается зависшей            |

Значение `GAMES_CDN_URLS` по умолчанию:
`https://cdn.gameap.ru/games.json,https://cdn.gameap.com/games.json`.

`DEFAULT_LANGUAGE` при чтении приводится к нижнему регистру. Испанский (`es`) и немецкий (`de`)
добавлены в 4.5.0. Значение действует, пока пользователь не выберет язык в интерфейсе: сначала
учитывается сохранённый выбор, затем `DEFAULT_LANGUAGE`, затем язык браузера.

## Проверка обновлений

Начиная с 4.5.0 панель запрашивает сведения о последних выпусках GameAP и GameAP Daemon и показывает
их администраторам в блоке версий на главной странице.

| Переменная             | Тип       | По умолчанию | Назначение                                                    |
|------------------------|-----------|--------------|----------------------------------------------------------------|
| `UPDATE_CHECK_ENABLED` | логич.    | `true`       | Проверять наличие новых выпусков GameAP и GameAP Daemon        |
| `UPDATE_CHECK_URLS`    | список    | см. ниже     | Источники выпусков, перебираются по порядку до первого ответа  |
| `UPDATE_CHECK_TTL`     | длительн. | `6h`         | Сколько кэшируется успешный результат                          |

Значение `UPDATE_CHECK_URLS` по умолчанию:
`https://cdn.gameap.com/{component}/releases.json,https://cdn.gameap.ru/{component}/releases.json,https://api.github.com/repos/gameap/{repo}/releases`.
`{component}` заменяется на `gameap` или `gameap-daemon`, `{repo}` — на имя репозитория GitHub,
`gameap` или `daemon`. Запрос прерывается через 15 секунд; неудачный результат кэшируется на
15 минут. При `UPDATE_CHECK_ENABLED=false` панель не делает исходящих запросов и показывает только
установленные версии. См. страницу [Обновление](/ru/upgrade.html).

## Переменные, которые не действуют

Эти переменные разбираются панелью, но на её поведение не влияют. Не рассчитывайте на них:

| Переменная                      | Что не так                                                                             |
|---------------------------------|-----------------------------------------------------------------------------------------|
| `AUTH_SESSION_IDLE_TIMEOUT`     | Завершения сессии по бездействию нет. Сессия живёт свой полный срок — 24 часа или 7 дней |
| `AUTH_SESSION_IDLE_UPDATE_FREQ` | То же самое                                                                             |
| `GRPC_FILE_TRANSFER_BASE_PATH`  | Значение доходит до конфигурации gRPC-сервера, но ни один обработчик его не читает       |
| `GRPC_ENABLED`                  | Такой переменной не существует вовсе. gRPC работает всегда, выключить его нельзя. Старые версии `gameapctl` дописывают эту строку в `config.env` |

Ограничения частоты входов тоже не настраиваются переменными окружения — они заданы константами
в коде. Значения приведены на странице [Безопасность](/ru/security.html).

## Пример минимальной конфигурации

```dotenv
DATABASE_DRIVER=postgres
DATABASE_URL=postgres://gameap:пароль@localhost:5432/gameap?sslmode=disable

AUTH_SECRET=замените_на_32_случайных_байта
ENCRYPTION_KEY=замените_на_32_случайных_байта

HTTP_HOST=panel.example.com
HTTP_PORT=8025
```

Всё остальное будет взято из значений по умолчанию. `ENCRYPTION_KEY` формально не обязателен, но без
него плагины не смогут хранить секреты: при `PLUGINS_SECRETS_REQUIRE_ENCRYPTION=true` (по умолчанию)
такие записи отклоняются. Длина ключа не проверяется: значение хешируется целиком, поэтому
32 случайных байта — рекомендация, а не требование.
